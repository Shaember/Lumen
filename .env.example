# Lumen Configuration
# Copy this file to .env and fill in the values

# REQUIRED: JWT secret for signing access tokens
# Generate with: openssl rand -hex 32
LUMEN_JWT_SECRET=

# CORS origin (default: http://localhost)
# Set to https://your-domain if using a real domain
LUMEN_CORS_ORIGIN=http://localhost
